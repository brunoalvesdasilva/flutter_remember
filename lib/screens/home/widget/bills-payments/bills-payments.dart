import 'package:flutter/material.dart';

class bills_payments extends StatefulWidget {
  const bills_payments({Key? key}) : super(key: key);

  @override
  _bills_paymentsState createState() => _bills_paymentsState();
}

class _bills_paymentsState extends State<bills_payments> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Bills Payment"),
    );
  }
}
